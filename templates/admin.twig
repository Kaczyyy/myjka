<h1>Panel administratora</h1>
<table border="1" style="width: 100%; text-align: left;">
    <tr>
        <th>ID</th><th>Data</th><th>Imię</th><th>Telefon</th><th>Email</th><th>Akcje</th>
    </tr>
    {% for r in reservations %}
        <tr>
            <td>{{ r.id }}</td>
            <td>{{ r.data }}</td>
            <td>{{ r.imie }}</td>
            <td>{{ r.telefon }}</td>
            <td>{{ r.email }}</td>
            <td><a href="?delete={{ r.id }}" onclick="return confirm('Na pewno usunąć?')">Usuń</a></td>
        </tr>
    {% endfor %}
</table>
